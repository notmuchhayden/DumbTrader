<UserControl x:Class="DumbTrader.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <GroupBox Header="계좌 정보" Margin="10">
            <DataGrid ItemsSource="{Binding Accounts}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="계좌번호" Binding="{Binding AccountNumber}" Width="*"/>
                    <DataGridTextColumn Header="계좌명" Binding="{Binding AccountName}" Width="*"/>
                    <DataGridTextColumn Header="상세명" Binding="{Binding AccountDetailName}" Width="*"/>
                    <DataGridTextColumn Header="별명" Binding="{Binding Nickname}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
