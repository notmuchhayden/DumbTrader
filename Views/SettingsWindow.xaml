<Window x:Class="DumbTrader.Views.SettingsWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Settings" Width="480" Height="320"
 WindowStartupLocation="CenterOwner"
 Background="{StaticResource Brush.Background}">
    <Grid Margin="12">
        <TextBlock Text="Settings" FontSize="18" Foreground="{StaticResource Brush.TextPrimary}" />
        <!-- Placeholder content -->
        <StackPanel Margin="0,36,0,0">
            <TextBlock Foreground="{StaticResource Brush.TextSecondary}" Text="(Put settings controls here)" />
            <Button Content="Close" Width="80" HorizontalAlignment="Right" Margin="0,12,0,0" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
