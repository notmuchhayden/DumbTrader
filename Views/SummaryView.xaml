<UserControl x:Class="DumbTrader.Views.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Background="#121A20"
            BorderBrush="{StaticResource Brush.Border}"
            BorderThickness="0,0,0,1"
            CornerRadius="8"
            Padding="20"
            Margin="16,16,16,0"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Style="{StaticResource TitleLarge}" Text="{Binding AccountId}"/>
            <TextBlock Grid.Column="2" Style="{StaticResource LabelText}" Text="Total Balance:"/>
            <TextBlock Grid.Column="4" Style="{StaticResource ValueGreen}" Text="{Binding TotalBalanceDisplay}"/>
        </Grid>
    </Border>
</UserControl>
