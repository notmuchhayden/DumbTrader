<UserControl x:Class="DumbTrader.Views.WatchlistView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="400" d:DesignWidth="700">

    <Grid Background="{StaticResource Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 조회 버튼: 화면 좌상단 -->
        <Button Content="조회" Width="100" Height="30" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding QueryStockListCommand}"/>

        <!-- 기존 메인 UI는 Row=1로 이동 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <!-- 관심종목 -->
            <Border BorderBrush="{StaticResource Brush.Border}" BorderThickness="2" Grid.Column="0" Background="{StaticResource Brush.Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>                    
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="관심종목" Style="{StaticResource TitleLarge}" HorizontalAlignment="Center" 
                               VerticalAlignment="Center" Grid.Row="0"/>
                    <DataGrid Grid.Row="1" Margin="0" Background="{StaticResource Brush.CardAlt}" 
                              BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" AutoGenerateColumns="False" 
                              HeadersVisibility="Column" Foreground="{StaticResource Brush.TextPrimary}" IsReadOnly="True"
                              ItemsSource="{Binding Watchlist}" CanUserResizeColumns="True" 
                              SelectedItem="{Binding SelectedWatchlist, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="종목명" Binding="{Binding hname}" Width="*" />
                            <DataGridTextColumn Header="단축코드" Binding="{Binding shcode}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- 버튼 영역 -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                <Button Width="40" Height="40" Content="&#x2190;" FontSize="20" Background="{StaticResource Brush.Card}" 
                        BorderBrush="{StaticResource Brush.Border}" Foreground="{StaticResource Brush.TextPrimary}" 
                        Margin="0,0,0,10" Command="{Binding SelectStockCommand}"/>
                <Button Width="40" Height="40" Content="🗑️" FontSize="20" Background="{StaticResource Brush.Card}" 
                        BorderBrush="{StaticResource Brush.Border}" Foreground="{StaticResource Brush.TextPrimary}" 
                        Command="{Binding RemoveWatchlistCommand}" />
            </StackPanel>

            <!-- 전체종목 -->
            <Border BorderBrush="{StaticResource Brush.Border}" BorderThickness="2" Grid.Column="2" 
                    Background="{StaticResource Brush.Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="전체종목" Style="{StaticResource TitleLarge}" HorizontalAlignment="Center" 
                               VerticalAlignment="Center" Grid.Row="0"/>
                    <!-- 검색 입력 및 버튼 추가 -->
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10,0,10,0" VerticalAlignment="Center">
                        <TextBox Width="200" Height="28" Margin="0,0,8,0" VerticalAlignment="Center" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Content="검색" Width="60" Height="28" VerticalAlignment="Center" Command="{Binding SearchCommand}" />
                    </StackPanel>
                    <DataGrid Grid.Row="2" AutoGenerateColumns="False" HeadersVisibility="Column" 
                              Background="{StaticResource Brush.CardAlt}" BorderBrush="{StaticResource Brush.Border}" 
                              BorderThickness="1" Foreground="{StaticResource Brush.TextPrimary}" 
                              ItemsSource="{Binding Stocks}" CanUserResizeColumns="True" IsReadOnly="True" 
                              SelectedItem="{Binding SelectedStock, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="종목명" Binding="{Binding hname}" Width="*" />
                            <DataGridTextColumn Header="단축코드" Binding="{Binding shcode}" Width="*" />
                            <DataGridTextColumn Header="확장코드" Binding="{Binding expcode}" Width="*" />
                            <DataGridTextColumn Header="ETF 구분" Binding="{Binding etfgubun}" Width="*" />
                            <DataGridTextColumn Header="구분" Binding="{Binding gubun}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
