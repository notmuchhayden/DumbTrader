<Window x:Class="DumbTrader.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DumbTrader.Views"
        xmlns:vm="clr-namespace:DumbTrader.ViewModels"
        mc:Ignorable="d"
        Title="Login" Height="340" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dumb Trader Login" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Username:" Margin="0,5"/>
        <TextBox Grid.Row="2" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="25"/>

        <TextBlock Grid.Row="3" Text="Password:" Margin="0,5"/>
        <PasswordBox Grid.Row="4" x:Name="UserPasswordBox" Margin="0,0,0,10" Height="25" VerticalAlignment="Top"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <CheckBox Grid.Row="5" Content="비밀번호 저장" IsChecked="{Binding SavePassword, Mode=TwoWay}" Margin="0,0,0,10"/>

        <Button Grid.Row="6" Content="Login" Width="100" Height="30" VerticalAlignment="Top" HorizontalAlignment="Center"
                Command="{Binding LoginCommand}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Grid>
</Window>
